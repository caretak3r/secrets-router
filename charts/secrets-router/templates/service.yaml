apiVersion: v1
kind: Service
metadata:
  name: {{ include "secrets-router.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "secrets-router.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secrets-router.selectorLabels" . | nindent 4 }}

